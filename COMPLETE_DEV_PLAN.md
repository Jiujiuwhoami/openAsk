# OpenAsk 100% 完成开发计划

> 目标：将 FEATURE_CHECKLIST.md 中所有 156 项功能全部实现
> 当前：99 项已完成 (63%)，57 项待开发
> 预估：全职开发约 8-12 周

---

## 目录

1. [Phase A：用户系统完善 (2周)](#phase-a用户系统完善)
2. [Phase B：商业化 (2周)](#phase-b商业化)
3. [Phase C：运营与分析 (2周)](#phase-c运营与分析)
4. [Phase D：嵌入与集成 (1周)](#phase-d嵌入与集成)
5. [Phase E：开发者体验 (1周)](#phase-e开发者体验)
6. [Phase F：电商场景 (2周)](#phase-f电商场景)
7. [Phase G：知识库 & 问答引擎 & 基础设施 (1周)](#phase-g知识库--问答引擎--基础设施)

---

## Phase A：用户系统完善

### A1 邮箱验证

**实现方案**：
- 后端新增 `POST /api/auth/send-verification` 发送验证邮件
- 后端新增 `POST /api/auth/verify-email?token=xxx` 验证邮箱
- 使用 JWT 作为验证 token（含邮箱、过期时间）
- 邮件发送使用 `resend` 或 `sendgrid` API（轻量，无需 SMTP）
- 前端：注册成功后提示"验证邮件已发送"
- 前端：用户中心可重新发送验证邮件

**新增文件**：
- `src/services/email_service.py` — 邮件发送服务
- `src/api/auth.py` 新增两个路由

**新增依赖**：
- `resend>=0.1.0` 或 `sendgrid>=6.0.0`

**配置**（`.env`）：
```bash
EMAIL_PROVIDER=resend
EMAIL_API_KEY=re_xxx
EMAIL_FROM=noreply@openask.dev
```

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 发送验证邮件 | 200 + 提示已发送 | POST /api/auth/send-verification |
| 未注册邮箱发送 | 404 | 不存在的邮箱 |
| 已验证邮箱重复发送 | 200 + 提示"已验证" | 已验证的邮箱再次发送 |
| 验证成功 | 200 + is_verified=true | 点击邮件链接 |
| 无效 token | 401 | 篡改 token |
| 过期 token | 401 | 24h 后的 token |
| 重复验证 | 200 + 提示"已验证" | 已验证后再次验证 |
| 邮件内容包含验证链接 | 链接可点击 | 查看邮件内容 |
| 发送频率限制 | 429 | 1 分钟内重复发送 |
| 注册后自动发送 | 注册成功提示"验证邮件已发送" | 注册新用户 |

---

### A2 密码重置

**实现方案**：
- `POST /api/auth/forgot-password` — 发送重置链接邮件
- `POST /api/auth/reset-password?token=xxx` — 重置密码
- 重置 token 使用 JWT，15 分钟有效期
- 前端：`/forgot-password` 页面 → 输入邮箱 → 发送邮件
- 前端：`/reset-password?token=xxx` 页面 → 输入新密码 → 提交

**新增文件**：
- `src/views/auth/ForgotPassword.vue`
- `src/views/auth/ResetPassword.vue`

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 发送重置邮件 | 200 | POST /api/auth/forgot-password |
| 不存在的邮箱 | 200（不暴露邮箱是否存在） | 同上 |
| 重置成功 | 200 + 新密码可登录 | 点击重置链接 → 设置新密码 |
| 重置后旧密码失效 | 401 | 用旧密码登录 |
| 无效 token | 401 | 篡改 token |
| 过期 token | 401 | 15 分钟后使用 |
| 重复使用 token | 401 | 重置后再次使用 |
| 前端重置页渲染 | 正常显示 | 访问 /reset-password |
| 前端忘记密码页渲染 | 正常显示 | 访问 /forgot-password |
| 重置后跳转登录 | 跳转 /login | 提交新密码 |

---

### A3 修改密码

**实现方案**：
- `POST /api/auth/change-password` — 需要旧密码 + 新密码
- 需要登录（JWT）

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 修改成功 | 200 | 正确旧密码 + 新密码 |
| 旧密码错误 | 401 | 错误旧密码 |
| 新密码太短 | 422 | 新密码 < 8 位 |
| 未登录 | 401 | 无 token |
| 修改后可用新密码登录 | 200 | POST /api/auth/token |
| 修改后旧密码失效 | 401 | 用旧密码登录 |

---

### A4 Token 刷新

**实现方案**：
- 登录时返回 `access_token` + `refresh_token`
- `POST /api/auth/refresh` — 用 refresh_token 换取新 access_token
- refresh_token 7 天有效期，一次性使用
- 前端：Axios 拦截器自动检测 401 → 尝试刷新 → 失败则跳转登录

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 登录返回 refresh_token | 响应包含 refresh_token | POST /api/auth/token |
| 刷新成功 | 200 + 新 access_token | POST /api/auth/refresh |
| refresh_token 过期 | 401 | 7 天后使用 |
| refresh_token 重复使用 | 401 | 使用一次后再次使用 |
| access_token 过期后自动刷新 | 请求成功 | 前端拦截器自动处理 |
| 刷新失败后跳转登录 | 跳转 /login | refresh_token 也过期 |

---

### A5 用户头像

**实现方案**：
- 接入 Gravatar（基于邮箱 hash 自动获取头像，零成本）
- 前端：`<el-avatar :src="gravatarUrl" />`
- 后备：首字母显示

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 头像显示 | 显示 Gravatar 头像 | 查看 Navbar 用户信息 |
| 无 Gravatar 账户 | 显示首字母 | 用未注册 Gravatar 的邮箱 |
| 头像缓存 | 头像加载快 | 二次访问 |

---

### A6 多语言支持

**实现方案**：
- 前端使用 `vue-i18n` 国际化库
- 支持中/英两种语言
- 语言切换存储在 localStorage
- 后端返回的文本错误信息也通过 Accept-Language 头判断

**新增依赖**：
- `vue-i18n>=10.0.0`

**新增文件**：
- `src/locales/zh-CN.ts`
- `src/locales/en-US.ts`

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 默认中文 | 界面显示中文 | 浏览器语言为中文 |
| 切换英文 | 界面显示英文 | 切换语言设置 |
| 切换后刷新保持 | 语言不变 | F5 刷新 |
| 登录页国际化 | 表单标签和提示翻译 | 切换语言查看 |
| 错误提示国际化 | 404/401 等提示翻译 | 触发错误 |

---

### A7 多设备登录 & 登录历史

**实现方案**：
- 多设备登录：默认支持（JWT 无状态，不限制设备数）
- 登录历史：新增 `login_history` 表，记录每次登录的 IP、User-Agent、时间
- 前端：`/security` 页面展示登录历史列表

**新增文件**：
- `src/views/auth/SecurityView.vue`

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 多设备登录 | 两台设备同时在线 | 不同浏览器登录 |
| 登录历史记录 | 显示 IP、时间、设备 | 查看安全页面 |
| 登录历史分页 | 超过 20 条分页 | 多次登录 |
| 清空历史 | 200 | 调用清空接口 |

---

## Phase B：商业化

### B1 套餐定义

**实现方案**：
- 三个套餐：Free / Pro ($29/月) / Enterprise ($99/月)
- 套餐限制存储在 `plan_limits` 表或配置文件中
- 每个 Project 关联一个 plan

**限制项**：

| 限制 | Free | Pro | Enterprise |
|------|------|-----|------------|
| 文档数 | 100 | 1,000 | 10,000 |
| 月问答次数 | 1,000 | 10,000 | 100,000 |
| 项目数 | 3 | 10 | 无限 |
| 自定义 LLM | ❌ | ✅ | ✅ |
| API 调用频率 | 10/min | 60/min | 300/min |
| 团队成员 | 1 | 3 | 10 |
| 嵌入脚本 | ✅ | ✅ | ✅ |
| 邮件支持 | ❌ | ✅ | 优先 |

**新增文件**：
- `src/services/plan_service.py` — 套餐定义与限制检查
- `src/domain/plan.py` — Plan 模型

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 注册默认套餐 | Free 套餐 | 注册后查看套餐 |
| 套餐限制生效 | 超限返回 429 | 超过文档数限制 |
| 项目数限制 | 超过限制返回 403 | 创建超过限制的项目 |
| 升级套餐后限制解除 | 可创建更多项目 | 升级后验证 |

---

### B2 Stripe 集成

**实现方案**：
- 使用 `stripe` Python SDK
- Webhook 处理：`checkout.session.completed`、`invoice.paid`、`subscription.deleted`
- 前端：跳转到 Stripe Checkout 页面
- 后端：`POST /api/billing/create-checkout-session` 创建结账会话
- `POST /api/billing/portal` 跳转客户门户
- `POST /api/stripe/webhook` 接收 Stripe 事件

**新增依赖**：
- `stripe>=7.0.0`

**新增文件**：
- `src/services/billing_service.py` — Stripe 集成
- `src/api/billing.py` — 计费相关路由
- `scripts/stripe_webhook_local.py` — 本地 webhook 测试

**配置**（`.env`）：
```bash
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_PUBLISHABLE_KEY=pk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
STRIPE_PRICE_FREE=price_free_xxx
STRIPE_PRICE_PRO=price_pro_xxx
STRIPE_PRICE_ENTERPRISE=price_enterprise_xxx
```

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 创建结账会话 | 200 + 返回 url | POST /api/billing/create-checkout-session |
| 未登录创建 | 401 | 无 token |
| Webhook 接收成功 | 200 | Stripe 发送事件 |
| 支付成功 → 升级套餐 | 套餐更新 | webhook 事件 |
| 取消订阅 → 降级 Free | 套餐降级 | webhook 事件 |
| 支付失败 → 发送提醒 | 邮件通知 | webhook 事件 |
| 客户门户 | 跳转 Stripe 门户 | POST /api/billing/portal |
| 当前套餐信息 | 200 + 套餐详情 | GET /api/billing/subscription |

---

### B3 用量计量

**实现方案**：
- 实时统计：每次 API 调用时记录用量（已有 stats 表）
- 月度重置：每月 1 日重置用量计数
- 限制检查：在 API 调用前检查是否超限

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 用量计数 | 调用后计数增加 | 调 /api/chat 后查 stats |
| 超限拒绝 | 429 | 超出套餐限制 |
| 月度重置 | 次月 1 日归零 | 修改系统日期测试 |
| 不同套餐不同限制 | Free 1000 次/月，Pro 10000 | 切换套餐测试 |

---

### B4 超量限制

**实现方案**：
- 软限制：超限时返回警告头 `X-Usage-Limit`，仍可继续使用
- 硬限制：超限后返回 402 Payment Required
- 前端：用量进度条，接近限制时告警

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 软限制警告头 | 响应包含 X-Usage-Limit | 接近限制时 |
| 硬限制拒绝 | 402 | 超出限制 |
| 升级后解除限制 | 请求正常 | 升级后立即调用 |
| 用量进度条显示 | 前端显示百分比 | 查看仪表盘 |

---

### B5 账单管理

**实现方案**：
- `GET /api/billing/invoices` — 历史账单列表
- 前端：账单列表页面，可下载 PDF

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 账单列表 | 200 + 账单数据 | GET /api/billing/invoices |
| 空账单 | 200 + [] | 刚注册用户 |
| 未登录 | 401 | 无 token |

---

### B6 套餐升降级

**实现方案**：
- 升级：立即生效，按比例收费
- 降级：下个周期生效
- 前端：套餐选择页面，对比表格

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 升级立即生效 | 限制解除 | Free → Pro |
| 降级下周期生效 | 限制暂不解除 | Pro → Free |
| 降级后超限 | 超过 Free 限制时拒绝 | 降级后文档数超过 100 |

---

### B7 开源版限制

**实现方案**：
- 开源版无限制（与 Enterprise 相同）
- 增加了 `OPENASK_EDITION=opensource` 环境变量
- 云托管版需要通过 Stripe 订阅

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 开源版无限制 | 可创建无限项目 | 设置环境变量 |
| 云托管版有限制 | 超限拒绝 | 未设置环境变量 |
| 开源版标识 | 页面显示"开源版" | 查看前端 |

---

## Phase C：运营与分析

### C1 问答量趋势

**实现方案**：
- 后端：`GET /api/projects/{id}/analytics/trends?days=30`
- 返回每日调用量、Token 消耗、缓存命中率
- 前端：ECharts 折线图（已有 `vue-echarts` 依赖）

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 趋势数据 | 200 + 每日数据 | GET /api/analytics/trends |
| 30 天数据 | 返回 30 个数据点 | 指定 days=30 |
| 空数据 | 200 + [] | 新项目 |
| 图表渲染 | 折线图正常显示 | 前端查看 |
| 图表交互 | 悬浮显示数据 | 鼠标悬停 |

---

### C2 热门问题 Top 10

**实现方案**：
- 后端：`GET /api/projects/{id}/analytics/top-questions`
- 统计查询频率，按次数降序排列
- 需要新建 `query_log` 表记录每次查询的 query 和 timestamp

**新增文件**：
- `src/services/analytics_service.py`

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| Top 10 返回 | 200 + 10 条 | 多次查询后 |
| 按频率排序 | 最高频第一条 | 验证排序 |
| 项目隔离 | 项目 A 看不到项目 B 的 | 交叉验证 |
| 空数据 | 200 + [] | 新项目 |

---

### C3 回答满意度

**实现方案**：
- 前端：每条回答底部 👍/👎 按钮
- 后端：`POST /api/chat/feedback` { message_id, rating: 'good'|'bad' }
- 后端：`GET /api/projects/{id}/analytics/satisfaction` — 满意度统计
- 需要新建 `feedback` 表

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 提交好评 | 200 | POST /api/chat/feedback { rating: 'good' } |
| 提交差评 | 200 | POST /api/chat/feedback { rating: 'bad' } |
| 满意度统计 | 200 + 好评率 | GET /api/analytics/satisfaction |
| 空数据 | 200 + 0% | 新项目 |
| 未登录 | 401 | 无 token |

---

### C4 服务端日志存储

**实现方案**：
- 新建 `chat_logs` 表：project_id, query, answer, sources, tokens, created_at
- 每次问答完成后自动记录
- `GET /api/projects/{id}/chat-logs?page=1&page_size=20`
- 前端：服务端日志页面替换 localStorage 方案

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 日志记录 | 问答后自动记录 | POST /api/chat 后查日志 |
| 日志列表 | 200 + 分页数据 | GET /api/chat-logs |
| 项目隔离 | 只返回当前项目日志 | 交叉验证 |
| 搜索日志 | 按 query 关键词筛选 | 搜索 |
| 时间范围筛选 | 按日期过滤 | 指定日期参数 |

---

### C5 日志导出

**实现方案**：
- `GET /api/projects/{id}/chat-logs/export?format=csv`
- 支持 CSV / JSON 格式

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| CSV 导出 | 200 + text/csv | GET /api/chat-logs/export?format=csv |
| JSON 导出 | 200 + application/json | GET /api/chat-logs/export?format=json |
| 空数据导出 | 200 + 空 csv | 新项目 |
| 导出文件名 | 包含项目名和日期 | 检查 Content-Disposition |

---

### C6 知识库审核流程

**实现方案**：
- 文档新增 `status: draft | pending | approved | rejected` 字段
- 编辑文档 → 状态变为 draft
- 提交审核 → 状态变为 pending
- 审核通过 → 状态变为 approved（知识库实际使用 approved 文档）
- 新增 `POST /api/knowledge/{id}/submit-review`
- 新增 `POST /api/knowledge/{id}/approve` / `reject`

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 创建文档状态 | draft | POST /api/knowledge |
| 提交审核 | pending | POST /api/knowledge/{id}/submit-review |
| 审核通过 | approved | POST /api/knowledge/{id}/approve |
| 审核拒绝 | rejected | POST /api/knowledge/{id}/reject |
| 检索只返回 approved | 不返回 draft | POST /api/search |
| 重新编辑后重置为 draft | draft | 编辑已 approved 文档 |

---

### C7 历史版本回滚

**实现方案**：
- 新建 `document_versions` 表：每次更新时保存旧版本
- `GET /api/knowledge/{id}/versions` — 版本列表
- `POST /api/knowledge/{id}/versions/{version_id}/restore` — 回滚

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 更新后生成版本 | 版本数 +1 | 更新文档后查版本列表 |
| 版本列表 | 200 + 版本列表 | GET /api/knowledge/{id}/versions |
| 回滚 | 文档内容恢复 | POST .../restore |
| 回滚后生成新版本 | 版本数 +1 | 回滚后查版本列表 |
| 删除文档后版本保留 | 版本仍可查 | 删除后查版本 |

---

### C8 知识库缺口分析

**实现方案**：
- 分析"答不上来"的问题（LLM 返回低置信度或用户点差评）
- `GET /api/projects/{id}/analytics/gaps` — 返回高频未解决问题
- 自动建议：基于未解决问题，推荐补充的文档主题

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 缺口列表 | 200 + 未解决问题 | GET /api/analytics/gaps |
| 按频率排序 | 高频问题优先 | 多次提问同一个问题 |
| 空数据 | 200 + [] | 新项目 |

---

### C9 敏感词过滤

**实现方案**：
- 新增敏感词库配置（可自定义）
- 用户提问时检查是否包含敏感词
- 包含敏感词时返回 400 + 提示信息
- 回答时过滤 LLM 输出中的敏感词

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 提问包含敏感词 | 400 | POST /api/chat { query: "敏感词" } |
| 回答过滤敏感词 | 回答中 *** 替换 | 提问触发敏感内容 |
| 敏感词库可配置 | 新增敏感词后生效 | 更新配置后测试 |
| 无害内容正常 | 200 | POST /api/chat { query: "正常问题" } |

---

## Phase D：嵌入与集成

### D1 嵌入脚本自定义

**实现方案**：
- 支持配置参数：`data-primary-color`、`data-position`（right/bottom）、`data-title`
- 在项目设置页增加可视化配置
- 生成的嵌入代码包含自定义参数

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 自定义主题色 | 聊天组件颜色改变 | 设置颜色后查看 |
| 自定义位置 | 组件位置改变 | 设置 position |
| 自定义标题 | 标题栏显示 | 设置 title |
| 配置实时生效 | 重新加载脚本后生效 | 刷新页面 |

---

### D2 电商平台集成

**D2.1 Shopify 集成**

**实现方案**：
- Shopify 应用：使用 Shopify CLI 创建嵌入式应用
- 使用 Shopify REST API 读取商品信息
- 自动将商品目录导入知识库
- OAuth 认证流程获取店铺权限

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| Shopify OAuth | 跳转授权页面 | 点击"连接 Shopify" |
| 商品同步 | 成功导入商品 | 查看知识库 |
| 增量同步 | 新商品自动导入 | 在 Shopify 新增商品 |
| 退换货政策 | 正确回答退换货问题 | 提问退换货流程 |

**D2.2 WooCommerce 集成**

**实现方案**：
- WordPress 插件形式
- 通过 WooCommerce REST API 同步商品
- 嵌入脚本直接集成到 WordPress 主题

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 插件安装 | 激活成功 | 安装插件 |
| API 连接 | 连接成功 | 配置 API Key |
| 商品同步 | 导入成功 | 查看知识库 |

---

### D3 第三方 Bot 集成

**D3.1 飞书 Bot**

**实现方案**：
- 飞书开放平台创建应用
- 使用飞书 Bot API 接收消息 → 调用 /api/chat → 回复
- 事件订阅机制

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 飞书消息接收 | 收到用户消息 | 在飞书发消息 |
| 回答回复 | 返回 AI 回答 | 提问 |
| 知识库隔离 | 按配置的项目回答 | 不同项目不同回答 |

**D3.2 钉钉 Bot**

类似飞书，使用钉钉开放平台 API。

**D3.3 企业微信 Bot**

类似飞书，使用企微开放平台 API。

**D3.4 Intercom 集成**

**实现方案**：
- Intercom 应用，使用 Intercom API
- 用户在 Intercom 中提问 → 调用 OpenAsk API → 回复

---

## Phase E：开发者体验

### E1 API 参考文档站

**实现方案**：
- 使用 `docsify` 或 `vitepress` 搭建 API 文档站
- 部署在 `/docs` 子域名或 `/docs` 路径
- 包含所有 API 的请求/响应示例
- 包含认证方式说明
- 包含错误码说明

**新增文件**：
- `docs/README.md`
- `docs/api/chat.md`
- `docs/api/knowledge.md`
- `docs/api/projects.md`
- `docs/api/auth.md`
- 等

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 文档站可访问 | 200 | 访问 /docs 路径 |
| 所有 API 有文档 | 无遗漏 | 对照路由列表 |
| curl 示例可运行 | 示例返回正确 | 复制示例运行 |
| 响应格式正确 | 与实际一致 | 对比文档和实际响应 |

---

### E2 SDK / 客户端库

**Python SDK**：
```
pip install openask-sdk
```

```python
from openask import OpenAsk
client = OpenAsk(api_key="sk_xxx")
response = client.chat("退货政策是什么？")
```

**JavaScript SDK**：
```javascript
import { OpenAsk } from 'openask-sdk'
const client = new OpenAsk({ apiKey: 'sk_xxx' })
const res = await client.chat('退货政策是什么？')
```

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| Python SDK 安装 | 安装成功 | pip install |
| Python SDK 问答 | 返回回答 | 调用 chat() |
| Python SDK 知识库管理 | 文档 CRUD 正常 | 调用 create_document() |
| JS SDK 安装 | 安装成功 | npm install |
| JS SDK 问答 | 返回回答 | 调用 chat() |

---

### E3 代码示例

**新增文件**：`examples/` 目录

```
examples/
├── curl/
│   ├── chat.sh
│   ├── knowledge.sh
│   └── projects.sh
├── python/
│   ├── chat.py
│   ├── knowledge.py
│   └── embed.py
└── javascript/
    ├── chat.js
    └── embed.html
```

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| curl 示例 | 可运行返回正确结果 | 复制运行 |
| Python 示例 | 可运行返回正确结果 | python chat.py |
| JS 示例 | 可运行 | node chat.js |

---

### E4 嵌入示例页面

**新增文件**：`examples/embed/demo.html`

一个完整的 HTML 页面，展示如何嵌入聊天组件，包含：
- 基础嵌入
- 自定义主题色
- 自定义位置
- 移动端适配

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 示例页面可访问 | 200 | 打开 demo.html |
| 嵌入组件显示 | 聊天按钮可见 | 页面加载后 |
| 自定义主题色生效 | 颜色改变 | 修改参数 |

---

## Phase F：电商场景

### F1 商品信息导入

**实现方案**：
- 支持 CSV / JSON 批量导入商品信息
- 自动将商品标题、描述、规格等转为知识库文档
- `POST /api/knowledge/import-products` — 上传商品文件
- 商品模板：`商品名称, 商品描述, 规格, 价格, 库存`

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| CSV 导入 | 成功创建文档 | 上传商品 CSV |
| 商品问答 | 正确回答商品信息 | 提问"XX 商品多少钱" |
| 导入进度 | 返回导入进度 | 大文件导入 |
| 重复导入 | 去重或覆盖 | 再次导入同一文件 |

---

### F2 订单查询模板

**实现方案**：
- 预置 System Prompt 模板：退换货流程、物流查询、退款政策
- 预置 FAQ 文档模板（已有 FAQ 文档）
- 用户可在项目设置中选择模板一键导入

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 选择模板 | 自动创建文档 | 选择"退换货流程"模板 |
| 模板问答 | 正确回答 | 提问"怎么退货" |
| 自定义模板 | 可编辑导入的模板 | 修改后保存 |

---

### F3 客服对话仪表盘

**实现方案**：
- 实时显示：今日咨询量、响应时间、满意度
- 趋势图：7 天/30 天趋势
- 热门问题排行
- 需要接入 chat_logs 数据

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 今日咨询量 | 显示今日数字 | 有数据时 |
| 响应时间 | 平均响应时间 | 多次问答后 |
| 趋势图 | 折线图显示 | 查看仪表盘 |
| 热门问题 | Top 10 列表 | 有数据时 |

---

### F4 人工客服转接

**实现方案**：
- 当 LLM 置信度低或用户连续追问 3 次时，提示"转接人工客服"
- 发送通知邮件/消息给客服人员
- 简单实现：收集转接请求 → 邮件通知

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 低置信度转接 | 提示转接 | 问知识库外的问题 |
| 连续追问转接 | 提示转接 | 连续追问 3 次 |
| 转接通知 | 邮件通知 | 配置通知邮箱 |
| 转接记录 | 记录转接历史 | 查看转接记录 |

---

### F5 多店铺管理

**实现方案**：
- 一个 User 可以创建多个 Project（当前已支持）
- 每个 Project 对应一个店铺
- 项目列表展示所有店铺
- 无需额外开发（当前架构已支持）

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 创建多个店铺 | 每个店铺独立 | 创建多个项目 |
| 店铺间隔离 | 数据互不可见 | 交叉验证 |
| 店铺切换 | 快速切换 | 导航栏下拉 |

---

### F6 多语言支持

**实现方案**：
- 知识库文档支持多语言（en/zh/ar 等）
- 问答时自动检测问题语言 → 检索对应语言文档
- 使用 LLM 进行翻译（可选）
- 前端：语言切换开关

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 中文提问中文回答 | 正确 | 提问中文 |
| 英文提问英文回答 | 正确 | 提问英文 |
| 混合语言 | 用提问语言回答 | 中英混合 |
| 无对应语言文档 | 降级到其他语言 | 知识库只有中文，英文提问 |

---

### F7 多时区 & 货币识别

**实现方案**：
- 项目设置中可配置时区
- 日志时间戳按项目时区显示
- 使用 LLM 自动识别问题中的货币单位（无需额外开发）

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 时区配置 | 日志时间正确 | 设置 UTC+8 |
| 货币识别 | 正确识别 | 提问"$50"、"50欧元" |
| 货币换算 | 回答中包含换算 | 提问"50美元是多少人民币" |

---

## Phase G：知识库 & 问答引擎 & 基础设施

### G1 批量操作

**实现方案**：
- `POST /api/knowledge/batch-delete` — 批量删除
- 请求体：`{ "doc_ids": ["id1", "id2"] }`
- 前端：表格增加 selection 列，选中后显示批量删除按钮

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 批量删除 | 200 + 删除数量 | 选择多个文档删除 |
| 空 ID 列表 | 400 | 提交空数组 |
| 含不存在的 ID | 200 + 部分成功 | 混合有效/无效 ID |
| 批量删除确认 | 弹窗确认 | 前端点击批量删除 |

---

### G2 文档去重

**实现方案**：
- 基于内容 SHA256 hash 去重
- 插入时检查 hash 是否已存在
- 存在时返回 409 + 提示"文档已存在"

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 完全重复内容 | 409 | 两次上传相同内容 |
| 微小差异 | 正常插入 | 修改一个字符 |
| 重复标题不同内容 | 正常插入 | 标题相同内容不同 |
| 去重提示 | 409 + 提示"文档已存在" | 重复上传 |

---

### G3 多模态增强

**实现方案**：
- 已有 `multimodal_service.py` 接口
- 支持图片输入：用户上传图片 → 多模态 LLM 识别 → 回答
- 前端：聊天输入框增加图片上传按钮

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| 图片上传 | 图片可上传 | 点击上传图片 |
| 图片识别 | 正确识别图片内容 | 上传商品图片 |
| 纯文本问答 | 不受影响 | 不传图片 |
| 大图片 | 压缩后上传 | 上传 10MB 图片 |

---

### G4 Prometheus 指标完善

**实现方案**：
- 已有 `prometheus-fastapi-instrumentator` 依赖
- 增加自定义指标：总调用次数、活跃项目数、缓存命中率
- 暴露 `/metrics` 端点

**验证测试**：
| 测试项 | 预期 | 方法 |
|--------|------|------|
| /metrics 可访问 | 200 | GET /metrics |
| 指标格式正确 | Prometheus 格式 | 检查内容格式 |
| 调用次数指标 | 请求后指标增加 | 调 /api/chat 后查看 |
| 缓存命中率指标 | 反映缓存状态 | 多次相同查询 |

---

## 附录：实现优先级

| 优先级 | 功能 | 预估时间 | 原因 |
|--------|------|----------|------|
| **P0** | 基础用户体验 | | |
| | A1 邮箱验证 | 2天 | 用户真实性 |
| | A2 密码重置 | 1天 | 基本功能 |
| | A3 修改密码 | 0.5天 | 基本功能 |
| **P1** | 商业化 | | |
| | B1 套餐定义 | 1天 | 收费基础 |
| | B2 Stripe 集成 | 3天 | 收费核心 |
| | B3 用量计量 | 1天 | 收费基础 |
| | B4 超量限制 | 1天 | 收费基础 |
| **P2** | 运营分析 | | |
| | C1 问答量趋势 | 1天 | 运营基础 |
| | C2 热门问题 Top 10 | 1天 | 运营基础 |
| | C3 回答满意度 | 2天 | 运营基础 |
| | C4 服务端日志 | 1天 | 运营基础 |
| **P3** | 开发者体验 | | |
| | E1 API 文档站 | 2天 | 开发者增长 |
| | E2 SDK | 3天 | 开发者增长 |
| | E3 代码示例 | 1天 | 开发者增长 |
| **P4** | 电商深度 | | |
| | F1 商品导入 | 2天 | 电商核心 |
| | F2 订单模板 | 1天 | 电商核心 |
| **P5** | 增强功能 | | |
| | C6 审核流程 | 2天 | 团队协作 |
| | C7 版本回滚 | 1天 | 数据安全 |
| | C8 缺口分析 | 2天 | 智能运营 |
| | C9 敏感词过滤 | 1天 | 合规 |
| | D1 嵌入自定义 | 1天 | 用户体验 |
| | G1 批量操作 | 1天 | 效率提升 |
| | G2 文档去重 | 0.5天 | 数据质量 |
| **P6** | 远期规划 | | |
| | D2 电商平台集成 | 2周 | 平台合作 |
| | D3 第三方 Bot | 1周/个 | 渠道拓展 |
| | F3 客服仪表盘 | 3天 | 运营深度 |
| | F4 人工转接 | 2天 | 服务闭环 |
| | F6 多语言 | 5天 | 国际化 |

---

*版本：v1.0 · 2026-08-06*